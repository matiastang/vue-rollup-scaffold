<svg id="mysvg" width="100" height="100" viewBox="0 0 100 100">
    <style type="text /css">
        circle {
        /* stroke-width: 5;
        stroke: #f00;
        fill: var(--themeColor);
        fill: #ff0; */
        }

        circle:hover {
        /* stroke: #090;
        fill: #fff; */
        }
    </style>
    <circle id="mycircle" cx="50" cy="50" r="40" />
</svg>